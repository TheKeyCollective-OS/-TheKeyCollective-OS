# Sprint 6B.3 — Canonical Corrective Baseline

Built directly from the ZIP downloaded from the published `main` branch.

- Repaired routes have a single interaction owner.
- Dashboard and Lani’s Corner use the same IndexedDB photo vault.
- Theme state is applied centrally to every route.
- Service-worker cache identity and loaded asset versions are synchronized.
- Older caches are removed during activation.
- Google OAuth still requires deployment credentials.
- Apple Calendar remains an `.ics` bridge.
